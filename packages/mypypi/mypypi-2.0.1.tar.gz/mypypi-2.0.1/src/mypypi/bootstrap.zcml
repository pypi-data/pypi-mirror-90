<configure
    xmlns="http://namespaces.zope.org/zope"
    i18n_domain="mypypi">

  <!-- Note
       This file must get loaded in app.zcml before any other zope.app package
       because subscribers depend on the order they get configured.
       -->
  <subscriber
      for="zope.app.appsetup.IDatabaseOpenedEvent"
      handler=".bootstrap.bootstrapPYPISite"
      />
</configure>
