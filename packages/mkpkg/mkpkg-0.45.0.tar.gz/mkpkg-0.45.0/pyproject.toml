[build-system]
requires = [
    "setuptools>=50.0.0",
    "setuptools_scm[toml]>=5.0",
    "wheel",
]
build-backend = "setuptools.build_meta"

[tool.isort]
from_first = true
include_trailing_comma = true
multi_line_output = 3

[tool.setuptools_scm]
