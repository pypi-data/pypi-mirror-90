[tool.poetry]
name = "lboxd"
version = "0.1.5"
description = "Letterboxd user tools."
authors = ["Your Name <you@example.com>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.8.5"
rich = "^9.6.1"
bs4 = "^0.0.1"
requests = "^2.0.0"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"

[tool.poetry.scripts]
greet = "lboxd.lboxd:main"
