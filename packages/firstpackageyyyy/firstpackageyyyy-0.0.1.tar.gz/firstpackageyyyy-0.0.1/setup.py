from setuptools import setup

setup(
    name='firstpackageyyyy',
    version='0.0.1',
    packages=['myapp']
)

# sdist: source code discribute, format: zip, gztar, bztar, xztar, ztar, tar; need compile in host machine
# bdist: bulit distribute, already compiled, install will get faster; format: gztar, bztar, xztar, ztar, tar, zip, rpm, pkgtool, sdux, wininst, msi
