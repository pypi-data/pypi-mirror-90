from setuptools import setup

setup(name='andreas_test_distributions',
      version='0.1',
      description='Gaussian distributions',
      packages=['andreas_test_distributions'],
      zip_safe=False)
