#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['rst_to_myst', 'rst_to_myst.data']

package_data = \
{'': ['*']}

install_requires = \
['docutils==0.15', 'importlib_resources~=3.1', 'pyyaml', 'click~=7.1']

extras_require = \
{'sphinx': ['sphinx~=3.2'], 'test': ['pytest~=6.0', 'coverage', 'pytest-cov']}

entry_points = \
{'console_scripts': ['rst2myst = rst_to_myst.cli:main']}

setup(name='rst-to-myst',
      version='0.1.1',
      description='Convert RST to MyST-Markdown.',
      author='Chris Sewell',
      author_email='executablebooks@gmail.com',
      url='https://github.com/executablebooks/rst-to-myst',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      extras_require=extras_require,
      entry_points=entry_points,
      python_requires='>=3.6',
     )
