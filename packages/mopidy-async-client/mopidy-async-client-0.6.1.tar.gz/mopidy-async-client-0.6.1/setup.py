#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['mopidy_async_client']

package_data = \
{'': ['*']}

install_requires = \
['websockets']

setup(name='mopidy-async-client',
      version='0.6.1',
      description='Async Mopidy Client via JSON/RPC Websocket interface',
      author='svinerus',
      author_email='svinerus@gmail.com',
      url='https://github.com/SvineruS/mopidy-async-client',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      python_requires='>=3.7',
     )
