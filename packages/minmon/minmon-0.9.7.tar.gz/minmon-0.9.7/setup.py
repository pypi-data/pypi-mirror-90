#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['minmon']

package_data = \
{'': ['*']}

install_requires = \
['psutil']

entry_points = \
{'console_scripts': ['minmon = minmon.__main__:main']}

setup(name='minmon',
      version='0.9.7',
      description='MINimum MONitor - write on stdout (and maybe on log file): timestamp, RAM and CPU usage, temperature, disk i/o and net i/o',
      author='Carlo Alessandro Verre',
      author_email='carlo.alessandro.verre@gmail.com',
      url='https://pypi.org/project/minmon',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      entry_points=entry_points,
      python_requires='>=3.7',
     )
