from setuptools import setup

setup(name='vizit',
      version='0.1',
      description='Visualization tool built on top of Numpy, Pandas, Matplotlib, and Seaborn',
      packages=['vizit'],
      zip_safe=False)