[tox]
envlist = py39,manifest,packaging

[testenv]
deps =
    pytest
    -rrequirements/main.txt
commands =
    pytest {posargs}

[testenv:manifest]
deps     = check-manifest
commands = check-manifest

[testenv:packaging]
deps     = pex
commands =
    pex . -c image-to-scan {posargs}
